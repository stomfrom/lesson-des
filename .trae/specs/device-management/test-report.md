# 第4章 系统测试

## 4.1 系统主要功能模块测试

### 4.1.1 设备管理功能测试

**组长：** 组长姓名

**1、测试方案**

设备管理功能是本系统的核心模块。该测试采用等价类划分法、边界值分析法、场景法等测试方法，针对设备管理页面中的查询设备（分页、按名称模糊搜索、按状态多选筛选）、新增设备（含输入校验）、查看设备详情、修改设备（含日期格式兼容）、删除设备等核心功能进行全面测试。

**2、测试用例**

表4-1 设备管理测试用例

| 测试编号 | 测试项 | 输入数据 | 预期结果 | 实际结果 | 结果分析 |
|---------|--------|---------|---------|---------|---------|
| TN-01 | 查询设备列表 | 不输入条件直接查询 | 分页展示全部设备 | 返回 `{code:200, list: [...]}` | 通过 |
| TN-02 | 按名称模糊搜索 | `name=电脑` | 展示名称匹配的设备 | 返回匹配结果 | 通过 |
| TN-03 | 按状态多选筛选 | `status=normal,maintenance` | 展示两种状态的设备 | 正确筛选 | 通过 |
| TN-04 | 分页参数 | `page=1, pageSize=10` | 返回第1页，每页10条 | `page=1, pageSize=10` | 通过 |
| TN-05 | 新增设备(合法) | name=测试设备, model=T-001, location=A区 | 创建成功，返回201 | 返回201及设备数据 | 通过 |
| TN-06 | 新增设备(缺型号) | 仅传name和location | 返回400提示必填 | 返回"型号必填" | 通过 |
| TN-07 | 新增设备(超长名称) | name=101个A | 返回400超限 | 返回"超出长度限制" | 通过 |
| TN-08 | 新增设备(无效状态) | status=invalid | 返回400枚举错误 | 返回"状态值无效" | 通过 |
| TN-09 | 新增设备(无效日期) | last_maintenance_date=bad-date | 返回400日期格式错 | 返回"维保日期格式无效" | 通过 |
| TN-10 | 查询详情(存在) | 已有设备的ID | 返回设备完整信息 | 返回设备详情 | 通过 |
| TN-11 | 查询详情(不存在) | ID=99999 | 返回404 | 返回"设备不存在" | 通过 |
| TN-12 | 查询详情(无效ID) | ID=abc | 返回400 | 返回"无效的设备ID" | 通过 |
| TN-13 | 查询详情(负数ID) | ID=-1 | 返回400 | 返回"无效的设备ID" | 通过 |
| TN-14 | 修改设备(合法) | 修改name/model/location | 更新成功返回200 | 更新成功 | 通过 |
| TN-15 | 修改设备(不存在) | ID=99999 | 返回404 | 返回"设备不存在" | 通过 |
| TN-16 | 修改设备(缺字段) | 仅传name | 返回400必填校验 | 返回"型号、位置必填" | 通过 |
| TN-17 | 修改设备(ISO日期) | last_maintenance_date=ISO格式 | 自动标准化为YYYY-MM-DD后保存 | 保存成功 | 通过 |
| TN-18 | 删除设备(正常) | 已有设备的ID | 删除成功返回200 | 返回"删除成功" | 通过 |
| TN-19 | 删除设备(不存在) | ID=99999 | 返回404 | 返回"设备不存在" | 通过 |

**3、测试结论：** 19项测试全部通过。设备的 CRUD、输入校验、边界条件均正确处理。

---

### 4.1.2 用户与权限管理功能测试

**组员1：** 组员1姓名

**1、测试方案**

用户与权限管理功能采用场景法和错误推测法，针对用户注册（输入合法性校验）、登录（正确/错误凭证）、用户列表查看（权限控制）、权限分配（事务保护）等功能进行测试。

**2、测试用例**

表4-2 用户与权限管理测试用例

| 测试编号 | 测试项 | 输入数据 | 预期结果 | 实际结果 | 结果分析 |
|---------|--------|---------|---------|---------|---------|
| TN-20 | 注册(合法) | username/ password/ nickname | 创建成功，返回201 | 返回201与token | 通过 |
| TN-21 | 注册(用户名重复) | username=admin | 返回409冲突 | 返回"用户名已存在" | 通过 |
| TN-22 | 注册(用户名太短) | username=ab | 返回400长度错误 | 返回"用户名长度3-50" | 通过 |
| TN-23 | 注册(密码太短) | password=12345 | 返回400长度错误 | 返回"密码至少6个字符" | 通过 |
| TN-24 | 登录(正确密码) | admin/admin123 | 登录成功返回JWT | 返回200及token | 通过 |
| TN-25 | 登录(错误密码) | admin/wrong | 返回401统一提示 | 返回"用户名或密码错误" | 通过 |
| TN-26 | 登录(不存在用户) | 不存在的用户名 | 返回401统一提示 | 返回"用户名或密码错误" | 通过 |
| TN-27 | 用户列表(admin) | admin请求/users | 返回用户列表 | 返回全部用户含权限 | 通过 |
| TN-28 | 用户列表(非admin) | operator请求/users | 返回403禁止 | 返回"仅管理员可访问" | 通过 |
| TN-29 | 设置用户权限 | resource=device, actions=[read,create] | 权限设置成功 | 返回"权限设置成功" | 通过 |

**3、测试结论：** 10项测试全部通过。认证模块正确区分正确/错误凭证，权限控制有效隔离管理员和普通用户的操作范围。

---

### 4.1.3 安全与输入校验测试

**组员2：** 组员2姓名

**1、测试方案**

安全性测试采用渗透测试思想，针对未认证访问、Token 校验、SQL 注入、请求体超限、昵称超长等攻击向量进行测试，验证系统的安全防护能力。

**2、测试用例**

表4-3 安全与输入校验测试用例

| 测试编号 | 测试项 | 输入数据 | 预期结果 | 实际结果 | 结果分析 |
|---------|--------|---------|---------|---------|---------|
| TN-30 | 未认证访问 | 不带Authorization header | 返回401 | 返回"未登录" | 通过 |
| TN-31 | 过期Token | 使用伪造的过期Token | 返回401 | 返回"无效的登录凭证" | 通过 |
| TN-32 | SQL注入(OR注入) | name=`' OR 1=1 --` | 参数化查询拦截，正常返回 | 正常返回，未注入 | 通过 |
| TN-33 | SQL注入(联合查询) | name=`' UNION SELECT * FROM users --` | 参数化查询拦截 | 正常返回 | 通过 |
| TN-34 | SQL注入(DROP尝试) | name=`x'; DROP TABLE users; --` | 参数化查询拦截 | 正常返回，users表未被删除 | 通过 |
| TN-35 | JSON超限 | 发送>10KB的请求体 | 服务端拒绝 | 返回413/400 | 通过 |
| TN-36 | 注册昵称超长 | nickname=60个字符 | 返回400校验错误 | 返回"昵称不能超过50个字符" | 通过 |

**3、测试结论：** 7项测试全部通过。参数化查询有效防御了三种 SQL 注入攻击向量；JWT 认证中间件正确识别无效 Token；请求体大小限制生效；输入校验覆盖了昵称超长等边界。

---

### 4.1.4 Dashboard 与生命周期测试

**1、测试方案**

Dashboard 功能测试验证统计数据接口返回数据的完整性和正确性，包括设备总数、各状态分布等关键指标。

**2、测试用例**

表4-4 Dashboard与生命周期测试用例

| 测试编号 | 测试项 | 输入数据 | 预期结果 | 实际结果 | 结果分析 |
|---------|--------|---------|---------|---------|---------|
| TN-37 | Dashboard统计数据 | 访问/devices/stats | 返回统计数据 | 返回total、byStatus等 | 通过 |

**3、测试结论：** 1项测试通过。Dashboard 统计接口正确聚合设备总数和各状态数量。

---

### 4.1.5 生产构建测试

**1、测试方案**

验证前端项目能够成功构建为生产环境产物，保证部署环节无误。

**2、测试用例**

表4-5 生产构建测试用例

| 测试编号 | 测试项 | 输入数据 | 预期结果 | 实际结果 | 结果分析 |
|---------|--------|---------|---------|---------|---------|
| TN-38 | npm run build | 无 | 构建成功，0 error | 构建成功，无错误 | 通过 |

**3、测试结论：** 1项通过。前端生产构建无报错，产物完整。

---

## 4.2 测试结论

本次系统测试共执行 **38 个测试用例**，覆盖设备管理 CRUD（19项）、用户与权限管理（10项）、安全与输入校验（7项）、Dashboard 统计（1项）、生产构建（1项）五大模块。

| 测试模块 | 测试用例数 | 通过 | 失败 |
|---------|-----------|------|------|
| 设备管理功能 | 19 | 19 | 0 |
| 用户与权限管理 | 10 | 10 | 0 |
| 安全与输入校验 | 7 | 7 | 0 |
| Dashboard与生命周期 | 1 | 1 | 0 |
| 生产构建 | 1 | 1 | 0 |
| **合计** | **38** | **38** | **0** |

**通过率：100%**

测试中未发现任何功能缺陷或安全问题。各项功能按需求规格正确实现，安全性措施（参数化查询/JWT认证/请求限流/输入校验）全部生效，生产构建通过。

**总体评价：** 系统功能完整、运行稳定、安全措施到位，达到项目预期目标，可交付使用。
